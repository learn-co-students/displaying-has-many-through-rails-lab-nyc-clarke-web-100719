<h1><%= @doctor.name %> - <%= @doctor.department %>
</h1>

<div>
    <ul>
        <% @doctor.appointments.each do|appointment| %>
            <li> <%= link_to "#{appointment.better_date} - #{appointment.patient.name}", patient_path(appointment.patient) %>
            </li>
        <% end %>
    </ul>
</div>
