<% @doctor.appointments.each do |apt| %>
<%= link_to apt.patient.name, patient_path(apt.patient)%>
<%= "#{apt.appointment_datetime.strftime("%B %d, %Y")} at #{apt.appointment_datetime.strftime("%H:%M")}" %>
<%end%>