<h1><%= @patient.name %></h1>

<ol>
    <% @patient.appointments.each do |appt| %>
        <hr><li><%=link_to "#{appt.appointment_datetime.strftime("%B %d, %Y")} at #{appt.appointment_datetime.strftime("%H:%M")} - #{appt.doctor.name}", doctor_path(appt.doctor) %></li><hr><br>
    <% end %>
</ol>